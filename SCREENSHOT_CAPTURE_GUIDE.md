# ChatRivet ストア用スクリーンショット撮影ガイド

Chrome Web Store用に、1280x800 PNGを3枚用意する。撮影はChatRivetを読み込んだテスト用Chromeプロフィールまたは個人情報を除去した画面で行う。

## 共通条件

- ブラウザ表示領域を1280x800にする。
- PNGで保存する。
- ブラウザのタブ、ブックマークバー、プロフィール名、通知、実際の会話名、アカウント名、メールアドレスを映さない。
- テスト用の会話タイトルだけを使う。例: `企画メモ`、`週次レビュー`、`資料作成`、`調査ノート`。
- ChatRivet以外の拡張機能、個人情報を含むプロジェクト名・サイドバー項目は非表示にする。
- 会話本文や入力欄を主役にせず、左サイドバーのChatRivetが分かる構図にする。

## 1. 自然な統合表示

目的: ChatRivetがChatGPTの左サイドバーへ自然に統合され、現在のチャットを追加できることを示す。

- ChatRivetが公式のチャット履歴付近の安全な位置に表示されている。
- ChatRivet見出しと、現在のチャットを追加する`+`ボタンが見える。
- 公式「ピン留め」がない環境でもChatRivetを表示できる仕様と矛盾しない構図にする。

正式ファイル名: `store-assets/ChatRivet-v1.2.0-store-01-basic.png`

## 2. 複数ピンの利用例

目的: 複数の重要チャットを独自一覧で整理できることを示す。

- ChatRivetにテスト用チャットを3件程度表示する。
- 追加ボタンが見える状態にする。
- 現在開いているテスト用チャットが分かる状態にする。

正式ファイル名: `store-assets/ChatRivet-v1.2.0-store-02-organize.png`

## 3. 公式「最近」との共存

目的: ChatRivet登録済みのチャットも公式「最近」に表示され、公式履歴と共存することを示す。

- ChatRivetにテスト用チャットを2件以上表示する。
- 公式「最近」を開く。
- 同じテスト用チャットがChatRivetと公式「最近」の両方に見える状態にする。
- 公式「最近」の「…」メニューが利用できることが分かる状態にする。
- 公式「最近」の見出しとChatRivetが別の独立セクションであることが分かる構図にする。

正式ファイル名: `store-assets/ChatRivet-v1.2.0-store-03-recent-coexist.png`

## Store Listingでの共通利用

この3枚は日本語と英語のStore Listingで共通利用する。画像内のUIを言語ごとに加工したり、旧v1.1.0画像と混在させたりしない。

## 撮影前の最終確認

- [ ] 3枚とも1280x800 PNGである。
- [ ] 3枚ともChatRivetの名称と主要機能が読める。
- [ ] 個人名、メールアドレス、実際の会話名、ブックマーク、プロフィール情報が写っていない。
- [ ] ChatGPTやOpenAIの公式提供物と誤認させる表現がない。
- [ ] 実際の現行版に存在しないフォルダ、長さインジケーターを示していない。
